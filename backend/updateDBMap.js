// updateDBMap.js — DEPRECATED
// The `lat` and `lng` columns are now included in the base schema (schema_update.sql / setupDB.js).
// This script is kept only for reference and should NOT be run on the updated database.

console.log('⚠️  updateDBMap.js is deprecated. The lat/lng columns are already in the schema.');
console.log('   Run schema_update.sql or setupDB.js instead.');
process.exit(0);
